"""String templates for the emitted suite's scaffolding (config, manifest, README).

Plain f-strings/`string.Template`, not a templating engine — the ladder rule
"fits in one line? standard lib does it?" applies to the tool's own code too.
"""
